const Schedule = () => {
  return (
    <div style={{
      display: 'flex',
      height: '100vh',
      alignItems: 'center',
      justifyContent: 'center',
      color: '#000'
    }}>
      placeholder
    </div>
  );
};

export default Schedule; 